function main()
    tmp = read_file("samples/example5.out")
    i = 0
    j = 0
    nrows = num_rows(tmp)
    ncols = num_columns(tmp)
    while (i < nrows)   # Acceso a la tabla por numero de row y nombre de columna
        tmp[i,"age"] = tmp[i,"age"] + 1
        i = i + 1
    end
end
