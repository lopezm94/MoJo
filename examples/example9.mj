function main()
    a = ["id","name","age"]
    t = create_table(a)
    dict = {"id":1,"name":"Marc the savage","age":22}
    add_row!(t,dict)
    rows = num_rows(t)
    if(rows = 1)
        write("Working")
    else 
        write("Not working")
    end
end
