function main()
    x = source("examples/example1.mj"); # Ejecuta la funcion main del script "example1.mj" con los parametros dentro de la lista
    if (x)
        writeln "Your instruction functions work!"
    else
        writeln "Check your language implementation <.<"
    end
end
